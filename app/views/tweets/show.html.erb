<h1>ツイート詳細</h1>


<p><span>email: </span><%= @user.email %></p>
<p><span>ツイート内容: </span><%= @tweet.body %></p>
<p><span>Image:</span><%= image_tag @tweet.img.url if @tweet.img?%></p>

<h2>コメント一覧</h2>
<% @tweet.comments.each do |comment| %>
  <p><span>コメント内容: </span><%= comment.content %></p>
  <%= link_to '編集' , edit_tweet_comment_path(@tweet, comment.id)%>
  <%= link_to '削除', [comment.tweet, comment], method: :delete, data: { confirm: "You sure?" } %>
<% end %>

<%= form_for [@tweet, @comment] do |f| %>
  <%= f.text_field :content %>
  <br>
  <%= f.submit 'コメントする' %>
<% end %>

<%= link_to "ホームへ戻る", tweets_path %>